if(PLATFORM STREQUAL "Desktop")
    include_directories(external/glad/include)
    if(OPENGL_VERSION STREQUAL "ES 2.0" OR OPENGL_VERSION STREQUAL "ES 3.0")
        set(GLAD_SOURCES external/glad/src/gles2.c)
    else()
        set(GLAD_SOURCES external/glad/src/gl.c)
    endif()
else()
    set(GLAD_SOURCES "")
endif()